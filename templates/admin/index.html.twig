{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
<div class="d-flex align-items-center justify-content-between mb-3">
  <div>
    <h1 class="h3 mb-1">{{ title }}</h1>
    <p class="text-muted mb-0">Seleziona cosa vuoi gestire.</p>
  </div>
  <a class="btn btn-outline-secondary" href="{{ path('app_home') }}"><- Home</a>
</div>

<div class="row g-3 align-items-stretch">
  <div class="col-12 col-md-6 col-lg-4">
    <div class="card h-100 shadow-sm border-0 opacity-75">
      <div class="card-body d-flex flex-column">
        <h2 class="h5 card-title mb-2">Contesti</h2>
        <p class="card-text text-muted flex-grow-1">
          Gestisci i contesti.
        </p>
        <button class="btn btn-outline-secondary" type="button" disabled>
          In arrivo
        </button>
      </div>
    </div>
  </div>

  <div class="col-12 col-md-6 col-lg-4">
    <div class="card h-100 shadow-sm border-0 opacity-75">
      <div class="card-body d-flex flex-column">
        <h2 class="h5 card-title mb-2">Direzioni</h2>
        <p class="card-text text-muted flex-grow-1">
          Gestisci le direzioni.
        </p>
        <button class="btn btn-outline-secondary" type="button" disabled>
          In arrivo
        </button>
      </div>
    </div>
  </div>

  <div class="col-12 col-md-6 col-lg-4">
    <div class="card h-100 shadow-sm border-0 opacity-75">
      <div class="card-body d-flex flex-column">
        <h2 class="h5 card-title mb-2">Lingue</h2>
        <p class="card-text text-muted flex-grow-1">
          Gestisci le lingue.
        </p>
        <button class="btn btn-outline-secondary" type="button" disabled>
          In arrivo
        </button>
      </div>
    </div>
  </div>

  <div class="col-12 col-md-6 col-lg-4">
    <div class="card h-100 shadow-sm border-0 opacity-75">
      <div class="card-body d-flex flex-column">
        <h2 class="h5 card-title mb-2">Livelli</h2>
        <p class="card-text text-muted flex-grow-1">
          Gestisci i livelli.
        </p>
        <button class="btn btn-outline-secondary" type="button" disabled>
          In arrivo
        </button>
      </div>
    </div>
  </div>

  <div class="col-12 col-md-6 col-lg-4">
    <div class="card h-100 shadow-sm">
      <div class="card-body d-flex flex-column">
        <h2 class="h5 card-title mb-2">Nuova traduzione</h2>
        <p class="card-text text-muted flex-grow-1">
          Crea una frase e aggiungi una o più traduzioni.
        </p>
        <a class="btn btn-primary" href="{{ path('app_admin_frasi_iten') }}">
          Vai al form
        </a>
      </div>
    </div>
  </div>
</div>
{% endblock %}
