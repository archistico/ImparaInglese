{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
<div class="row g-3 align-items-stretch">
    <div class="col-12">
        <div class="p-4 bg-white border rounded-3 shadow-sm">
            <h1 class="h3 mb-1">{{ title }}</h1>
            <p class="text-muted mb-0">
                Scegli cosa vuoi allenare oggi. Per ora partiamo con le frasi più utili (Italiano → Inglese)
            </p>
        </div>
    </div>

    {# Card 1 - attiva #}
    <div class="col-12 col-md-6 col-lg-4">
        <div class="card h-100 shadow-sm">
            <div class="card-body d-flex flex-column">
                <h2 class="h5 card-title mb-2">Frasi Italiano → Inglese</h2>
                <p class="card-text text-muted flex-grow-1">
                    Seleziona un contesto (presentarsi, ristorante, hotel…) e prova a tradurre
                </p>
                <a class="btn btn-primary"
                    href="{{ path('app_frasi_iten') }}">
                    Vai ai contesti
                </a>
            </div>
        </div>
    </div>

    {# Card 2 - attiva #}
    <div class="col-12 col-md-6 col-lg-4">
        <div class="card h-100 shadow-sm">
            <div class="card-body d-flex flex-column">
                <h2 class="h5 card-title mb-2">Frasi Inglese -> Italiano</h2>
                <p class="card-text text-muted flex-grow-1">
                    Allenati a capire frasi reali in inglese.
                </p>
                <a class="btn btn-primary"
                    href="{{ path('app_frasi_enit') }}">
                    Vai ai contesti
                </a>
            </div>
        </div>
    </div>

    {# Card 3 - disabilitata #}
    <div class="col-12 col-md-6 col-lg-4">
        <div class="card h-100 shadow-sm border-0 opacity-75">
            <div class="card-body d-flex flex-column">
                <h2 class="h5 card-title mb-2">Come comporre le frasi</h2>
                <p class="card-text text-muted flex-grow-1">
                    Strutture base, ordine delle parole, domande, negazioni
                </p>
                <button class="btn btn-outline-secondary" type="button" disabled>
                    In arrivo
                </button>
            </div>
        </div>
    </div>

    {# Card 4 - disabilitata #}
    <div class="col-12 col-md-6 col-lg-4">
        <div class="card h-100 shadow-sm border-0 opacity-75">
            <div class="card-body d-flex flex-column">
                <h2 class="h5 card-title mb-2">Verbi e tempi verbali</h2>
                <p class="card-text text-muted flex-grow-1">
                    Present simple, past, future, modali… con esempi pratici
                </p>
                <button class="btn btn-outline-secondary" type="button" disabled>
                    In arrivo
                </button>
            </div>
        </div>
    </div>

    {# Card 5 - disabilitata #}
    <div class="col-12 col-md-6 col-lg-4">
        <div class="card h-100 shadow-sm border-0 opacity-75">
            <div class="card-body d-flex flex-column">
                <h2 class="h5 card-title mb-2">Mini dizionario</h2>
                <p class="card-text text-muted flex-grow-1">
                    Parole utili e collocazioni, con esempi
                </p>
                <button class="btn btn-outline-secondary" type="button" disabled>
                    In arrivo
                </button>
            </div>
        </div>
    </div>

    {# Card 6 - attiva #}
    <div class="col-12 col-md-6 col-lg-4">
        <div class="card h-100 shadow-sm">
            <div class="card-body d-flex flex-column">
                <h2 class="h5 card-title mb-2">Admin</h2>
                <p class="card-text text-muted flex-grow-1">
                    Aggiungi nuove frasi, contesto, ...
                </p>
                <a class="btn btn-primary"
                    href="{{ path('app_admin_home') }}">
                    Amministrazione
                </a>
            </div>
        </div>
    </div>
</div>
{% endblock %}
